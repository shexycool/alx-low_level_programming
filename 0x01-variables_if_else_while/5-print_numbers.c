#include <stdio.h>
#include <stdlib.h>
/**
 * main - entery point
 *
 * Return: Always 0
 */
int main(void)
{
	char n = 10;

	char r;

	if (r == 0; r < n;)
		putchar(r);
		r++;
	putchar('\n');
	return (0);
}
